# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
venv/
.venv/
env/
.env
.env.*
!.env.example

# Node / Vercel
node_modules/
.vercel/
# Note: frontend/assets/js/env.js ships a safe empty default and is
# regenerated at build time from API_BASE_URL — keep it tracked.

# IDE
.vscode/
.idea/
.claude/
*.swp
*.swo

# OS
Thumbs.db
.DS_Store

# Logs
*.log

# Scratch scripts
scratch/

# ── Legacy pre-refactor code (NOT deployed; superseded by backend/ + frontend/) ──
# Leading slashes anchor these to the repo root so the real backend/frontend
# copies (e.g. backend/modules, frontend/assets) are NOT ignored.
# You can delete these local files at any time — see README "Legacy cleanup".
/main.py
/web_main.py
/config.py
/vercel.json
/Procfile
/requirements.txt
/requirements-desktop.txt
/gui/
/assets/
/web/
/modules/
/services/
/utils/
/data/
