# Rebuilds /breaches/ from Have I Been Pwned, then tells the search engines.
#
# The generator is the bulk of the job: it fetches the catalogue, writes the
# article pages, the JSON feed the app reads, and the sitemap entries.
# Committing the result is what triggers a Vercel deploy, so the site updates
# itself without anyone touching it.
#
# It commits only when something actually changed — a no-op run leaves no
# commit behind, which keeps the history readable and avoids a deploy that
# ships nothing.
name: Breach Files

on:
  schedule:
    # Every six hours, off the hour. Whoever publishes the first useful page
    # on a new breach collects the links, so being early matters more here
    # than it does on evergreen pages — and GitHub queues scheduled jobs
    # hardest exactly on the hour, where a delay costs exactly that lead.
    - cron: "15 */6 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: breach-files
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Generate articles
        working-directory: frontend
        run: node scripts/build-breaches.js

      - name: Commit if anything changed
        id: commit
        run: |
          if [ -z "$(git status --porcelain)" ]; then
            echo "No breach updates this run."
            echo "changed=" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Which article URLs actually changed. Worked out before the commit,
          # while git status still reports them as dirty. IndexNow asks for
          # changed URLs only — re-submitting untouched pages on every run is
          # how a domain gets its submissions throttled or ignored.
          CHANGED=$(git status --porcelain frontend/breaches | awk '{print $NF}' | grep -E '^frontend/breaches/.*[.]html$' | sed 's|^frontend|https://www.myrecon.xyz|' | tr '\n' ' ')
          echo "changed=$CHANGED" >> "$GITHUB_OUTPUT"

          git config user.name "myrecon-bot"
          git config user.email "bot@myrecon.xyz"
          git add frontend/breaches frontend/assets/data/breaches.json frontend/sitemap.xml
          git commit -m "Breach Files: scheduled update"
          git push

      - name: Tell the search engines
        # Runs after the push, because the push is what triggers the deploy.
        # Submitting first would send a crawler to the previous version of the
        # page, which is worse than not submitting at all.
        if: steps.commit.outputs.changed != ''
        working-directory: frontend
        run: node scripts/indexnow.js ${{ steps.commit.outputs.changed }}
